"""Analytics sub-package — deterministic statistical engines.

Lives at the same level as ``agents/`` (outside ``app/``) so it can be
imported by both service code and agent code without circular dependencies.
"""
